# Edge Functions

Serverless functions deployed to Supabase Edge (Deno runtime, free tier:
500k invocations/month). None exist yet — Phase 1 is pure client → Supabase
via RLS. This folder is where Phase 3 logic (e.g. Claude AI integration)
will live.

Create one with:

```sh
supabase functions new <name>
```
