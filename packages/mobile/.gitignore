# Expo / React Native build artefacts
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects, when generated by prebuild
/ios
/android

# Local env — the anon key is public by design, but the file is still per-machine
.env
.env.local
